# Task: re-verify the D6 fixes on the real app (short run)

You are Codex running on macOS with the computer-use tool. Use it for every interaction with the app; use the shell only for builds, file work and `screencapture`. Do the work synchronously yourself; do not delegate, schedule or ask. Never use `GRAPHENE_DEBUG`, the `cmd.txt` debug driver, or scripted UI automation inside the app. Known limits of your API (no pointer-only move, no held-modifier click, coordinate drags fail): skip those states and say so; never substitute a wrong capture.

## Build and launch
1. `git status`; note HEAD (untracked files under `docs/parity/` are fine; never delete, stash or commit anything).
2. `GRAPHENE_APP_DIR=$PWD/.build/Graphene-verify.app ./scripts/build-app.sh`. Never touch `~/Applications/Graphene.app`; never quit a Graphene process you did not start.
3. Launch from a shell that stays alive (`… & wait`) with `GRAPHENE_DATA_DIR=/tmp/graphene-identity-fixes` (use a fresh directory name if it exists; do not delete anything). Record the PID. Default onboarding. Window 1280×820. Window id via `swift scripts/window-id.swift -p <PID>`; capture with `screencapture -l <id> -o -x <file>.png` into `docs/parity/shots/identity-fixes/`; verify each PNG with `sips` and by viewing it.

## Checks (public pages only)
1. **Resume entry.** Open https://en.wikipedia.org/wiki/Graphene. Click the sidebar "+ New Tab" row: `newtab-row.png` must show the Resume page (search row, Continue with the Graphene thread) in a new blank tab, not the command bar. Type `swift`: `newtab-typing.png` must show the command bar opened with "swift" typed. Escape. Click the footer "+": same result (`newtab-footer.png`). ⌘T over the Graphene page: `cmdt.png` must show the command bar (unchanged Arc behaviour).
2. **Branch.** From the Graphene page use the link context menu → Open Link in New Window (as in the previous run) on Carbon and on Graphite, then from Carbon on one more link. `branch.png`: root, two children, one grandchild; measure the tick: it must run from the vertical line to the child's icon-slot edge (x 17→28 from the row's leading edge, i.e. 34→56 px). Select Graphene and use the Tabs menu → Collapse Branch (⌃⌥⌘←) while the web page has focus: `branch-collapsed.png` must show Graphene alone with a trailing count "3" and the connector gone; Expand Branch (⌃⌥⌘→): `branch-expanded.png`. Hover a parent row if you can: the favicon swaps for a chevron (`branch-chevron.png`, else say hover is unavailable).
3. **Ask citations.** On the Graphene tab press ⌘K. `ask-empty.png`: grounding line "This page" with an accent dot; the notices line under the sources strip must read like "Page text trimmed to 6,000 characters" (record the exact text), not "1 source warnings". Ask "What is graphene's tensile strength and who first isolated it?" and wait. `ask-answer.png`: the answer must carry inline numbered chips and a sources line, and the page must show highlighted passages with matching superscripts. Click chip 1: `ask-click.png` shows the page scrolled to and the mark active. Record the answer text verbatim. Ask "What is the capital of Peru?": `ask-nosources.png` (the "No sources" line; note if the answer is wrong). Close the panel: `ask-closed.png` with no marks left.
4. **Shelf.** Triple-click a paragraph on the Graphene page, ⌘D, Save. Do the same on https://forums.swift.org. `shelf.png`: two chips with a 12pt favicon each; record the visible quote fragment of each (it must start at a content word, e.g. "Swift…" not "The S…"), the chip width in points, and the leading glyph (a tray glyph labelled Vault). Click a chip: `shelf-open.png` (source scrolled to the mark).

## Report
`docs/parity/shots/identity-fixes/report.md`: HEAD, PID, build/test tails, a PASS / FAIL (observed) / FAIL (blocked) table for every numbered check with evidence files, the recorded texts and measurements, anything that looked wrong, and cleanup (quit your PID, delete `.build/Graphene-verify.app`). Be blunt. Do not claim what you did not observe.
