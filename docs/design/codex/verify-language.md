# Task: verify the Graphene design-language surfaces (G6–G9) on the real app

You are Codex running on macOS with the computer-use tool. Use it for every interaction with the app; use the shell only for builds, file work and `screencapture`. Do the work synchronously yourself; do not delegate, schedule or ask. Never use `GRAPHENE_DEBUG`, the `cmd.txt` debug driver, or scripted UI automation inside the app. Known API limits (no pointer-only move, no held-modifier click, coordinate drags often fail): skip those states and say so; never substitute a wrong capture. Public pages only.

## Build and launch
1. `git status`; note HEAD (untracked files under `docs/parity/` are fine; never delete, stash or commit anything).
2. `GRAPHENE_APP_DIR=$PWD/.build/Graphene-verify.app ./scripts/build-app.sh`. Never touch `~/Applications/Graphene.app`; never quit a Graphene process you did not start.
3. Launch from a shell that stays alive (`… & wait`) with a fresh `GRAPHENE_DATA_DIR=/tmp/graphene-language-verify` (pick a new name if it exists). Default onboarding. Window 1280×820. Window id via `swift scripts/window-id.swift -p <PID>`; capture with `screencapture -l <id> -o -x <file>.png` into `docs/parity/shots/language/`; verify each PNG with `sips` and by viewing it.

## Seed
Open https://en.wikipedia.org/wiki/Graphene. From it, use the link context menu → Open Link in New Window on Carbon and on Graphite; from Carbon do the same on one more link. Triple-click the Graphene opening paragraph, ⌘D, Save. On the Carbon page select a paragraph, ⌘D, add the note "for the intro", Save.

## Checks
**A. Thread map (G6).** Open Threads (⌘⌥2 or the footer library glyph). `map.png`: the Graphene thread as a tree: Graphene at column 0, Carbon and Graphite at column 1 directly under it, the grandchild at column 2 under Carbon; one connector path from Graphene with ticks into both children and a second from Carbon; the node with a Vault note shows a 6pt accent dot; a start-time label above the root column; no timestamps on nodes; no zoom control. Click the grandchild: `map-selected.png` shows its ancestor path in accent. Right-click or use the hover card (if reachable) → "Resume from here" on Carbon: `map-resumed.png` and then the sidebar must show Carbon and its child as Today tabs with the child indented (`map-resumed-sidebar.png`). Switch to List with the library-bar glyph: `map-list.png` rows indented by depth. Measure column pitch (180pt) and row pitch (40pt).

**B. Annotations and Vault (G7).** On the Graphene page, `mark.png`: the saved paragraph is marked with the accent tint (same style as citations). Select a sentence: `selection-bar.png` must show a floating card with Save ⌘D, Note, Ask (no brand name), 36pt tall, above the selection, in the page's colour scheme. Click Note, type "test", Save note: `note-editor.png` before saving. If hover is available, hover the mark for the margin dot (`margin-dot.png`), else skip. Open Vault: `vault.png` must be a list, not a grid: each note as quote in serif with a left rule, note text under it where present, a provenance line (favicon, title, space, relative date), no hairlines between rows. Click the first quote: `vault-open.png` shows the page scrolled to the mark. Toggle "By page": `vault-by-page.png` groups notes under their page with a depth-1 hairline tree. Select a row: the library bar shows Edit, Delete, Copy as Markdown, Add to Board.

**C. Ask (G8).** On the Graphene page ⌘K, ask "What is graphene's tensile strength and who first isolated it?" `ask-answer.png`: chips per claim; the mark for the tensile-strength claim must be the infobox row only (not the whole infobox), and the isolation claim must mark the 2004 sentence. Click the isolation chip: `ask-click.png` shows the 2004 sentence centred in the uncovered part of the page with the active tint. Record the chip help texts. `ask-empty-new.png`: a new chat shows exactly the line "Attach a page with @ or ask about this one."

**D. Board (G9).** Open Board (⌘⌥5). `board-empty.png`: lattice canvas with "Drop tabs, notes and quotes here." Drag the Graphene tab and the Carbon tab from the sidebar onto the canvas if drags work; otherwise use Add link twice with their URLs. `board-cards.png`: two page cards (favicon, title, host, thumbnail or placeholder) snapped to 28pt cells and a connector from Graphene's right edge to Carbon's left edge, since Carbon was opened from Graphene. Move Carbon (drag or Add note then compare): the connector follows (`board-moved.png`). Drop a Vault shelf chip or use Vault → Add to Board: `board-quote.png` shows a quote card in serif with the left rule and a provenance line. Delete Graphene: `board-deleted.png` shows the connector gone.

**E. Mail (G9).** Open Mail (⌘⌥3). Without OAuth it shows the disconnected web fallback; `mail-disconnected.png` must show the library-bar line "Read-only. Graphene never sends mail." If a connected account is available in this profile (it is not, unless already configured), also capture rows and a threaded conversation; otherwise mark blocked.

**F. Language rules.** In every capture above, note any hairline that is not a provenance connector or a quote rule, any serif on chrome text, any lattice outside empty Threads/Vault/new-tab and the Board canvas.

## Report
`docs/parity/shots/language/report.md`: HEAD, PID, build/test tails, a PASS / FAIL (observed) / FAIL (blocked) table for every check with evidence files, measurements, the "What still doesn't read as one product" list ordered by visual impact, crashes/hangs/console errors, and cleanup (quit your PID, delete `.build/Graphene-verify.app`). Be blunt; do not claim what you did not observe.
